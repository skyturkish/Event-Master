# Discord bot token (Bot tab in the Discord Developer Portal).
DISCORD_TOKEN=your-bot-token-goes-here

# Discord application (client) ID — used to deploy slash commands.
CLIENT_ID=your-application-id-goes-here

# Base URL of the Event-Master backend API.
# Falls back to "http://localhost:3000" if not set.
BASE_URL=http://localhost:3000
